# --enable-gtk3 added in build(): the template's --with-gtk detection looks
# for the wrong flag name for this package - it uses --enable-gtk3, not
# --with-gtk - so it silently built (or tried to) against GTK2, which ScrapLinux
# does not package.
