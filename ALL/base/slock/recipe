# slock - Simple X screen locker
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=slock
version=1.5
release=1
desc="Simple X screen locker"
url="https://tools.suckless.org/slock/"
license="MIT"
depend="libx11 libxext libxrandr libxcrypt"
makedepend="bmake"
source="https://dl.suckless.org/tools/slock-1.5.tar.gz"
sha256="SKIP"

build() {
	cd "slock-1.5"
	# Suckless software is configured in the source. ScrapLinux ships a config.h
	# with the ScrapLinux palette; replace it and rebuild to change anything.
	[ -f config.def.h ] && cp -f config.def.h config.h
	make -j"$JOBS" PREFIX=/usr
}

package() {
	cd "slock-1.5"
	make PREFIX=/usr DESTDIR="$pkgdir" install
	[ -f config.h ] && install -Dm644 config.h "$pkgdir/usr/share/slock/config.h"
	true
}
