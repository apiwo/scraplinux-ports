# kiwmi - Lua-scriptable Wayland compositor
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=kiwmi
version=0-20220730
release=1
desc="Lua-scriptable Wayland compositor"
url="https://github.com/buffet/kiwmi"
license="MIT"
depend="wlroots wayland libxkbcommon pixman lua"
makedepend="meson ninja"
source="https://github.com/buffet/kiwmi/archive/392b6c38fd189e304aa97f9c6fc88a5c7113a140.tar.gz"
sha256="SKIP"

build() {
	cd "kiwmi-392b6c38fd189e304aa97f9c6fc88a5c7113a140"
	meson setup build \
		--prefix=/usr \
		--libdir=/usr/lib \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--buildtype=release \
		--wrap-mode=nodownload \
		-Db_lto=true
	ninja -C build -j"$JOBS"
}

package() {
	cd "kiwmi-392b6c38fd189e304aa97f9c6fc88a5c7113a140"
	DESTDIR="$pkgdir" ninja -C build install
}
