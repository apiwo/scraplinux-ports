# tinywl - Minimal reference Wayland compositor, wlroots' own example
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=tinywl
version=0.20.2
release=1
desc="Minimal reference Wayland compositor, wlroots' own example"
url="https://gitlab.freedesktop.org/wlroots/wlroots"
license="MIT"
depend="wlroots wayland libxkbcommon"
makedepend="pkgconf"
source="https://gitlab.freedesktop.org/wlroots/wlroots/-/releases/0.20.2/downloads/wlroots-0.20.2.tar.gz"
sha256="SKIP"

build() {
	cd "wlroots-0.20.2"
	make -j"$JOBS" PREFIX=/usr
}

package() {
	cd "wlroots-0.20.2"
	make PREFIX=/usr DESTDIR="$pkgdir" install
}
