# glycin is a Rust image-loading library and gdk-pixbuf 2.44 enables it by
# default on Linux. ScrapLinux has no Rust toolchain, so it is turned off here -
# the built-in loaders are what the rest of the stack actually uses.
