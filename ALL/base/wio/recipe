# wio - Wayland compositor similar to Plan 9's rio
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=wio
version=0.19.0
release=1
desc="Wayland compositor similar to Plan 9's rio"
url="https://github.com/Rubo3/wio"
license="MIT"
depend="wlroots wayland libxkbcommon cage"
makedepend="meson ninja"
source="https://github.com/Rubo3/wio/archive/refs/tags/0.19.0.tar.gz"
sha256="SKIP"

build() {
	cd "wio-0.19.0"
	meson setup build \
		--prefix=/usr \
		--libdir=/usr/lib \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--buildtype=release \
		--wrap-mode=nodownload \
		-Db_lto=true
	ninja -C build -j"$JOBS"
}

package() {
	cd "wio-0.19.0"
	DESTDIR="$pkgdir" ninja -C build install
}
