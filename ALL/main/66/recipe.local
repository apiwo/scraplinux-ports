# CATCHLOG=0/VERBOSITY=1 sed fixes in package() and the release=2 bump that
# ships them are hand edits, not something gen-ports.py's template can
# express - the manifest has no field for post-install sed patches.
# Regenerating this recipe from the manifest would silently drop both.
