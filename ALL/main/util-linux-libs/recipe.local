# Libraries only, enforced twice: --disable-all-programs in build() and a
# removal of every binary directory in package(). A full `make install` here
# shipped util-linux's /bin/login, which links libpam - a library ScrapLinux does
# not have - over busybox's working login applet, and no installed system
# could be logged into. The template has no field for either.
