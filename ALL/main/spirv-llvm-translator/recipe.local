# The tests are off and FetchContent is disconnected: the build otherwise
# git-clones SPIRV-Headers mid-configure, which needs network from inside the
# sandbox and pins a copy of headers ScrapLinux already packages.
