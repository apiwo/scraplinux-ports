# scraplinux-base - ScrapLinux base configuration, init and branding
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=scraplinux-base
version=1.0.0
release=1
desc="ScrapLinux base configuration, init and branding"
url="https://github.com/apiwo/scraplinux"
license="BSD-2-Clause"
depend="busybox toybox zsh doas scraps libxcrypt"
makedepend=""
source=""
sha256=""

# A meta package: the dependency list is the whole payload. Installing it pulls
# in a working setup and nothing else.
package() {
	install -d "$pkgdir/usr/share/scraplinux/meta"
	printf '%s\n' "busybox toybox zsh doas scraps libxcrypt" >"$pkgdir/usr/share/scraplinux/meta/scraplinux-base"
}
